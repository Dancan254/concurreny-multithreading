package org.javaguy.maps;

import java.util.HashMap;

public class Main {
    static void main() {
        //list- > 1, 2, 3, 4 -> O(n) -> contains
        //Set -> contains -> O(1)
        //array -> arr[90] -> baseAddress +
        System.identityHashCode(new HashMap<>());
    }
}
